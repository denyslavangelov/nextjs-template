{
  "systemPrompt": "Follow the rules in .cursor/README.md by default. Keep changes minimal and focused. Do not rewrite full files unless necessary. Only load other .cursor/*.md files when explicitly relevant."
}