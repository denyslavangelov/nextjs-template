{
  "systemPrompt": "Follow the rules in .cursor/context.md by default. Keep changes minimal and focused. Do not rewrite full files unless necessary. Only load other .cursor/*.md files when explicitly relevant. For UI tasks, always apply the design quality, typography, and anti-generic-AI rules. For visible copy, always apply the copywriting rules, especially native Bulgarian copy when the content is in Bulgarian."
}
